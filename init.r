library(shiny)
library(shinythemes)

library(DT)

library(dplyr)

library(plotly)
library(ggplot2)
library(ggcorrplot)

library(lattice)
library(caret)

options(shiny.host = '0.0.0.0')
options(shiny.port = 3030)

url = "dataset"